# Required for the AI blueprint and visual-generation stages.
EMERGENT_LLM_KEY=replace_with_your_key

# A MongoDB database is used for background render jobs and history.
MONGO_URL=mongodb://localhost:27017
DB_NAME=faceless_forge

# For a same-domain container deployment, keep this as *.
CORS_ORIGINS=*
